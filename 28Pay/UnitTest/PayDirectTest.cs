using System;
using Libs.TopupPartner.VNPTEPAY;
using Microsoft.VisualStudio.TestTools.UnitTesting;
using Libs.TopupPartner.PayDirect;

namespace UnitTest
{
    [TestClass]
    public class PayDirectTest
    {
        [TestMethod]
        public void downloadSoftpin()
        {
            //PayDirectService es = new PayDirectService();
            //var requestid = DateTime.Now.ToString("yyyyMMddHHmmss");
            //var result = es.downloadSoftpin(requestid, "VTT", 10000, 2);
            //Console.WriteLine("Requestid= "+ requestid + " | Result= " + result);
        }
    }
}
