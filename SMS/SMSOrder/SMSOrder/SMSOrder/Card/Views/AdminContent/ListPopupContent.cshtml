@using SMS.Data.DTO
@model List<Contents>
@using SMS.Utility
@using SMS.CMS.Helper
@{ var idx = 0; }
<table class="table table-striped table-bordered table-hover dataTable no-footer" id="dataTables">
    <thead>
        <tr class="rowsao">

            <th style="text-align:left;">STT</th>
            <th style="text-align:left;">Tên</th>
            @*<th>Trạng Thái</th>*@
            @*<th>Ghim đầu trang</th>
                <th>Ngày xuất bản</th>*@
            <th>Tác vụ</th>
        </tr>

    </thead>
    @if (Model != null && Model.Count > 0)
    {

        foreach (var obj in Model)
        {
            { idx++; }
    <tr>

        <td style="text-align:left;">@(idx + (ViewBag.CurrentPage - 1) * ViewBag.PageSize )</td>
        <td style="text-align:left;">
            <a href="javascript:;" onclick="SetData('@obj.Description')"> @obj.Title</a>

        </td>

        <td style="text-align:left">
            <a href="javascript:;" onclick="SetData('@obj.Description')"> Chọn</a>


        </td>
    </tr>
        }
    }
</table>

<script type="text/javascript">
    function SetData(data) {
        $('#hdContent').val(data);
        $('.btn-success').click();
       
    }
    var PageOnSize = '@ViewBag.PageSize';
    var CurrentPage = '@ViewBag.CurrentPage';
     $(document).ready(function () {

        $("#pager").pager({
            pagenumber: CurrentPage,
           pagecount: Math.ceil('@ViewBag.TotalRecord' / PageOnSize),
            buttonClickCallback: function (page) { BindDataRequest(page); }
        });


    });
    //$('#dataTables').DataTable({
    //    "bPaginate": false,
    //    "searching": false,
    //    "ordering": true,
    //    "info": false
    //});
</script>

