using System;
using System.Collections.Generic;
using System.Linq;
using System.ServiceProcess;
using System.Text;

namespace EmailService
{
    internal static class Program
    {
        /// <summary>
        /// The main entry point for the application.
        /// </summary>
        private static void Main()
        {
            
            //var failOverProcess = new ServiceBase[] {new FailOverProcess()};
            //ServiceBase.Run(failOverProcess);

            //var autoBuyCardProcess = new ServiceBase[] { new AutoBuyCardProcess() };
            //ServiceBase.Run(autoBuyCardProcess);
        }
    }
}
